#include <string.h>
#include "main.h"

/**
 * _strcmp - function that compares two strings
 * @s1: first string
 * @s2: second string
 *
 * Return: returns 0 if strings are equal, otherwise if not equal
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
