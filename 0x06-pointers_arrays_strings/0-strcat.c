#include <string.h>
#include "main.h"

/**
 * _strcat - function to concatenate two strings
 * @dest: destination string
 * @src: source string
 *
 * Return: program returns a concatenated string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
