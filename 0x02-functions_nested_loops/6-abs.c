#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @x: The number to be computed
 *
 * Return: program returns absolute value
 */
int _abs(int x)
{
	if (x < 0)
		return -(x);
	return x
}
