#include <string.h>
#include "main.h"

/**
 * _strlen - it returns the length of a string
 * @s: the string
 *
 * Return: program returns the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
